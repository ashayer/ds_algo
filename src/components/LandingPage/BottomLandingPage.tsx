import { Grid, Typography, Button, Dialog } from "@mui/material";

const BottomLandingPage = () => {
  return <Grid item sx={{ backgroundColor: "#121212" }}></Grid>;
};

export default BottomLandingPage;
